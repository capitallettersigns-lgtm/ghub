# Optional environment overrides for daily-task-digest.
# Everything is normally configured in config.json; this file is only for overrides.

# Point the scripts at a config file other than ./config.json:
# CONFIG_PATH=./config.json
